# Production QA report — 30 August 2026

## Scope

This report records evidence gathered from the recovered C-drive repository,
the local static preview and the public canonical domain. It deliberately
distinguishes verified results from account or integration steps that still
require an owner-only Google action.

## Passed checks

| Area | Evidence |
| --- | --- |
| Source integrity | JavaScript syntax passed for the website and Apps Script; Apps Script validation, URL normalisation, header-safety and formula-safety unit checks passed; the manifest/web manifest parse as JSON and the sitemap parses as XML |
| Structured data | All six JSON-LD blocks parse; Organization/ProfessionalService, WebSite, WebPage, Service and FAQ claims correspond to visible page content |
| Links and assets | All repository-relative `href`, `src`, `poster` and deferred `data-src` targets exist; same-page anchors resolve |
| Public routes | `/`, `/privacy.html`, `/terms.html`, `/sitemap.xml`, `/robots.txt` and `/favicon.ico` return HTTP 200 over the canonical HTTPS host |
| Deployment | Production commits through `1ed72af` deployed successfully; Node 20 action warnings were removed by moving to the current official Node 24 majors |
| Domain and TLS | GoDaddy apex and `www` resolve to GitHub Pages; HTTP and apex variants redirect to canonical HTTPS `www`; the active certificate covers both hosts |
| Search foundation | Search Console Domain property is verified; the sitemap succeeds with three discovered URLs; homepage, apex, privacy and terms live tests succeeded and indexing requests were queued |
| Search presentation source | Baholo title, description, canonical URL, favicon family, Open Graph and Twitter card metadata are present at stable production paths |
| Accessibility fundamentals | One descriptive H1, logical section headings, landmark/region names, skip link, labelled form controls, visible focus handling, reduced-motion rules and descriptive image alternatives are present |
| Mobile responsive behaviour | Chrome at 390×844 reported no horizontal content overflow; desktop navigation was hidden, the menu control opened the mobile panel with correct ARIA state, hero video sources remained unassigned, hidden form mechanics stayed hidden and no console warning/error was logged |
| Form client behaviour | Empty submission focuses the first invalid field and announces an error; a valid local form with no endpoint announces the email/WhatsApp fallback rather than losing data silently |
| Consent and analytics | No GA script loads before consent; it loads once after consent on production only; advertising consent remains denied; GA4 Realtime showed the production title, page views and one intentionally triggered `cta_click` |
| Privacy and terms | Footer and form link to matched privacy and terms pages; consent can be changed from the footer; POPIA/legal review boundaries are stated |
| Performance controls | Below-fold images have intrinsic dimensions and lazy/deferred loading; the hero poster is WebP and preloaded; hero video is not sourced on mobile, Data Saver, 2G or reduced-motion contexts |
| PageSpeed mobile lab audit | PageSpeed Insights on the canonical production homepage scored 92 Performance, 100 Accessibility, 100 Best Practices and 100 SEO; FCP 0.9 s, LCP 3.0 s, TBT 0 ms, CLS 0 and Speed Index 4.1 s |
| Security baseline | No high-confidence secret pattern or tracked credential file was found in the current tree/reachable history; no browser code contains a Sheet ID, password or Google credential |

## Performance observations

- HTML, CSS and JavaScript are dependency-free static files of approximately
  49 KB, 63 KB and 20 KB respectively before transfer compression.
- The two optional desktop hero video encodings are approximately 1.73 MB and
  2.15 MB. Only the browser-supported source is fetched, and neither source is
  assigned under constrained/mobile conditions.
- All content images are compressed WebP except the intentional brand/source
  files. An older tracked `world-2.jpg` is unreferenced and therefore creates no
  page request; it remains recoverable history rather than an active payload.
- A public PageSpeed API request was initially rate-limited and the local
  Lighthouse process could not complete within the host's available memory.
  The PageSpeed Insights web audit subsequently completed against
  `https://www.baholoprojects.co.za/` using Lighthouse 13.4.1, an emulated Moto
  G Power and slow 4G. It reported 92 Performance, 100 Accessibility, 100 Best
  Practices and 100 SEO, with FCP 0.9 s, LCP 3.0 s, TBT 0 ms, CLS 0 and Speed
  Index 4.1 s. These are lab measurements, not field Core Web Vitals; Search
  Console field data should still be monitored after sufficient real traffic
  exists.

## Open production validations

1. **Apps Script/Sheet/email:** source and local validation are complete. The
   owner must finish the Google consent screen, after which `setup()`, web-app
   deployment and one labelled end-to-end test can be completed.
2. **Lead analytics:** `generate_lead` is registered as a GA4 key event but can
   only be verified after the live backend returns a newly stored reference.
3. **Business Profile:** the service-business draft is saved but not public.
   Replace Google's broad automatic `South Africa` service area with accurate
   named areas and complete verification using the real private operating or
   mailing address.
4. **Google search result:** source metadata and favicon are corrected, but the
   stale GoDaddy/Airo apex result remains under Google's recrawl timing. A live
   Google search already showed the terms page with its correct Baholo title,
   but the homepage result must not be called corrected until the stored apex
   result is reprocessed.
5. **Legal sign-off:** the matched privacy notice and terms require review by a
   qualified South African privacy/legal professional, including the stated
   enquiry-retention period and responsible-party contact process.

## Release gate

The static website, domain, TLS, search foundation and consented analytics are
production-ready. The lead workflow is not production-complete until the live
Apps Script deployment passes the Sheet, administrator-email, visitor-email,
duplicate-reference and GA4 key-event checks above.
